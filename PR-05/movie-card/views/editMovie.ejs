<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Movie</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f0f4f8;
            font-family: 'Arial', sans-serif;
        }
        .container {
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin-top: 50px;
        }
        .btn-success {
            background-color: #5a1968;
            border-color: #5a1968;
        }
        .btn-success a {
            color: #ffffff;
            text-decoration: none;
        }
        .btn-success:hover {
            background-color: rgb(151, 25, 136);
            border-color: #701484;
           
        }
        .btn-primary {
            background-color: #3e064a;
            border-color: #7b358a;
        }
        .btn-primary:hover {
            background-color: #7e4c89;
            border-color: #3e064a;
        }
        .form-label {
            font-weight: 500;
            margin-bottom: 0.5rem;
        }
        .form-control {
            border-radius: 8px;
            box-shadow: inset 0 1px 2px rgba(0, 0, 0, 0.075);
        }
        .form-control:focus {
            border-color: #007bff;
            box-shadow: 0 0 0 0.2rem rgba(7, 27, 49, 0.25);
        }
        .text-center {
            font-size: 1.75rem;
            font-weight: 600;
            margin-bottom: 20px;
        }
        .card {
            border: none;
        }
        .card-header {
            background-color: #8e34b2;
            color: #ffffff;
            font-size: 1.5rem;
            border-bottom: 5px solid #8358b1;
        }
        .card-body {
            padding: 2rem;
        }
    </style>
</head>

<body style="background-color: rgb(92, 72, 104);">


    <div class="container mt-4">
        <nav class="navbar navbar-light bg-light mb-4">
            <a class="navbar-brand" href="/">view page</a>
        </nav>

        <div class="card">
            <div class="card-body">
                <h1 class="text-center mb-4">Edit Movie</h1>

                <form action="/UpdateMovie" method="post" enctype="multipart/form-data">
                    <input type="hidden" name="editid" value="<%= single.id %>">

                    <div class="mb-3">
                        <label class="form-label">Name:</label>
                        <input type="text" class="form-control" name="name" value="<%= single.name %>" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Description:</label>
                        <input type="text" class="form-control" name="description" value="<%= single.description %>" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Price:</label>
                        <input type="number" class="form-control" name="price" value="<%= single.price %>" step="0.01" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Image:</label>
                        <input type="file" class="form-control" name="image">
                        <% if (single.image) { %>
                        <img src="<%= single.image %>" alt="Current Image" class="img-preview mt-2">
                        <% } %>
                    </div>

                    <button type="submit" class="btn btn-primary">Update Movie</button>
                    <a href="/" class="btn btn-success ms-2">Back to Movies</a>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
